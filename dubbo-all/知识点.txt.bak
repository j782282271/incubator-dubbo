1）如何保证同步调用？rocketmq是通过furture
2）本地存根本地伪装
3）Activate与Adaptive
4）delay的作用：
如果服务需要预热时间，比如初始化本地缓存，等待相关资源就位等，可以使用delay进行延迟暴露。使Dubbo在Spring容器初始化完后延迟多少毫秒再暴露服务。
delay没配置或者配置-1或者一个正数，都是开启delay:
如果delay为正数如5000，则会在afterPropertiesSet中暴露服务(延迟5s后暴露服务)，
如果delay是非-1的负数或者0，会会在afterPropertiesSet中立即(不延迟)暴露服务，
如果delay没配置或者配置-1，则会在Spring容器初始化完通知事件中暴露服务

5）设计模式
https://my.oschina.net/90888/blog/1645096

